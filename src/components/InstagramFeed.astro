---
const intro = {
  upperHeading: "Stay Connected",
  heading: "Follow Us on Instagram",
  headingText: [
    "Want to see what life at Sun City Aviation Academy is really like? Our Instagram is full of behind-the-scenes photos, student highlights, flight training moments, and special events.",
    "Join our growing community and get an inside look at how we train, celebrate, and inspire future pilots every day.",
  ],
  buttonText: "Follow us on Instagram",
  buttonLink: "https://www.instagram.com/suncity_aviation",
};

const { content = intro } = Astro.props;
---

<section id="instagram-feed" class="bg-white lg:py-24 overflow-hidden">
  <div class="mx-auto max-w-7xl px-4 lg:px-8">
    <div class="flex flex-wrap justify-evenly gap-2 items-start align-middle">
      <div class="w-full lg:w-2/3 lg:max-w-2xl px-4 mt-12 lg:mt-0 lg:px-12">
        <div
          class="elfsight-app-32aa28ed-6b18-4b4f-99ca-b5e1b65791d7"
          data-elfsight-app-lazy
        >
        </div>
      </div>
      <div
        class="w-full lg:w-1/3 flex flex-col items-center lg:items-start lg:sticky top-52"
      >
        {
          content.upperHeading && (
            <p class="text-xl mb-2 font-sans text-center uppercase lg:text-left font-black tracking-tight text-primary-800 bg-accent-300 w-fit px-4">
              {content.upperHeading}
            </p>
          )
        }
        {
          content.heading && (
            <h2 class="text-5xl lg:text-6xl font-sans text-center lg:text-left font-bold tracking-tight text-primary-950">
              {content.heading}
            </h2>
          )
        }
        {
          content.headingText.map((text) => (
            <p class="mt-4 text-lg font-sans text-center lg:text-left font-normal text-gray-700">
              {text}
            </p>
          ))
        }
        {
          content.buttonText && content.buttonLink && (
            <a
              href={content.buttonLink}
              target="_blank"
              class="mt-12 btn-accent text-center lg:text-left"
            >
              {content.buttonText}
            </a>
          )
        }
      </div>
    </div>
  </div>
</section>

<script src="https://elfsightcdn.com/platform.js" async></script>
